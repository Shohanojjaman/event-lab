import HeroSection from '../Components/utilities/HeroSection';

const Home = () => {
  return (
    <div>
      {/* Hero / Banner Section */}
      <HeroSection></HeroSection>
    </div>
  );
};

export default Home;
